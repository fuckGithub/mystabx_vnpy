{# 模块文档（验收 A10 + D9）：输出到插件根 backend/app/plugin/{{ package_name }}/README.md #}
# {{ package_name }} — {{ function_name }}

## 模块定位

{{ function_name }}。由代码生成器创建的**可选插件**，可整目录删除；边界：只提供表
`{{ table_name }}` 的 CRUD 接口与菜单/按钮权限登记。

## 入口

| 入口类型       | 路径                          | 说明                                                                              |
| -------------- | ----------------------------- | --------------------------------------------------------------------------------- |
| 插件入口       | `plugin.py`                   | `class Plugin(PluginBase)`；`MODEL_PATHS` 声明模型，`PLUGIN = Plugin()` 不可省     |
| HTTP 路由      | `{{ module_name }}/controller.py` | `{{ class_name }}Router`，容器前缀 `/{{ api_route_prefix }}/{{ module_name }}`（由 `app/core/discover.py` 扫描挂载） |
| WebSocket 路由 | 无                            | —                                                                                 |
| ORM 模型       | `{{ module_name }}/model.py`  | `{{ class_name }}Model`，表 `{{ table_name }}`                                     |
| 种子数据       | 无                            | —                                                                                 |
| 内核槽位       | 无                            | 既不提供也不消费                                                                   |
| 定时任务       | 无                            | 未调用 `ctx.add_scheduler_job`                                                     |
| 事件订阅       | 无                            | 未调用 `ctx.on(...)`                                                               |

## 依赖

- 内核槽位：无
- 其它插件：`system`（认证/权限基元 `AuthSchema`/`AuthPermission`，见 `plugin.toml` 的 `depends`）

## 删除影响

删除本目录后：

- 路由前缀 `/{{ api_route_prefix }}/{{ module_name }}` 下全部接口消失
- 表 `{{ table_name }}` 不再自动创建（已存在的表不会被删除，数据保留）
- 菜单「{{ function_name }}」及其按钮权限不再写入（已写入的行保留）
- 无种子、无定时任务、无内核槽位受影响
