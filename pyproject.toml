[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mystabx_vnpy"
version = "0.0.0"
description = "Scaffold on main; full prior implementation is on branch vnpy."
license = { text = "LicenseRef-Stabx-Source-Available" }
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = []
