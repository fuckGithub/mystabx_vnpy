---
description: 本仓库每完成 3 个用户任务合并为 1 次提交
alwaysApply: true
---

# 每 3 个任务提交一次

「3」= 3 个用户任务（一条可见独立需求），不是 3 次文件编辑，也不是 3 次 commit。

计数器文件：`~/.cursor/git-task-commit-counter.json`，形如 `{ "count": 0, "threshold": 3 }`。缺失则创建 `count: 0`。只读写此文件。

## 何时提交

1. 每完成 1 个任务：`count += 1` 并写回。`count < 3`：不 commit。
2. `count >= 3` 且有真实代码改动：把积压改动合并成 **1 次 commit**（本仓库至多 1 个）；有 remote 则 push；然后 `count = 0`。
3. 用户说提交 / 提报 / 推送：立刻 commit 一次并 `count = 0`。

## 禁止

- 每个任务各 commit 一次
- 按 Write/StrReplace/Delete 次数触发
- 消息写成 `Update 文件名`

## 安全

Never stage `.env` / secrets。Never `--no-verify`。Never 改 git config。Never force push。Never 改写已推送历史。
